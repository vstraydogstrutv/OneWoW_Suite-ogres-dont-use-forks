.releases/
.gitignore
.git/
.cache/

# Ricky's Cursor rules — local only, never commit or push (AccountSync, Triggers, Gitmode, CHANGELOG, etc.)
# `git add` will refuse these unless you force; keep them out of the shared repo.
.cursor/rules/1W-Ricky-*.mdc
.cursor/rules/Git-Commit-Rules.mdc

# Local secrets (never commit)
.env
.env.*

# Local Discord automation (tokens, channel IDs; keep out of repo)
scripts/Load-DiscordEnv.ps1
scripts/Send-ChangelogToDiscord.ps1

# Ephemeral Discord API payloads (Send-ChangelogToDiscord.ps1)
.discord-changelog-send.json*

# Local Documentation - Never Commit
guiplanner.md
apiplanner.md
dbplanner.md


# Local Developer Tools - Never Commit
OneWoW_Utility_Extractor/
OneWoW_UI/
bin/updatefolder/
ONEWOW_SUITE.code-workspace

# Audit tools: ignore everything EXCEPT the two published report files
bin/audit-tools/*
!bin/audit-tools/reports/
bin/audit-tools/reports/*
!bin/audit-tools/reports/summary.json
!bin/audit-tools/reports/AUDIT_SUMMARY.md

# Local game prototypes (Godot - not part of the addon suite) - Never Commit
games/

# Local reference / archive (R5 Production Files) - Never Commit
6_Last WN Addons/

# Local R7 Production Folder - Never Commit
ONEWOW_SUITE_7/
